<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KhyamAdhikari_COMP304_001_HandsOn_Test1_F24"
        tools:targetApi="31">

        <!-- // Student ID: 300868207 Main Activity (Logo Screen) -->
        <activity
            android:name=".KhyamActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KhyamAdhikari_COMP304_001_HandsOn_Test1_F24">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- // Student ID: 300868207 Adhikari Activity (Add Contacts Screen) -->
        <activity
            android:name=".AdhikariActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KhyamAdhikari_COMP304_001_HandsOn_Test1_F24" />

    </application>
</manifest>
